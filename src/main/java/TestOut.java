import java.util.function.Function;

public class TestOut {
    public static Object add(Object a, Object b) {
        if (a instanceof Integer && b instanceof Integer) {
            return (Integer) a + (Integer) b;

        }
        if (a instanceof Float && b instanceof Float) {
            return (Float) a + (Float) b;

        }
        if (a instanceof Double && b instanceof Double) {
            return (Double) a + (Double) b;

        }
        if (a instanceof String && b instanceof String) {
            return (String) a + (String) b;

        }
        throw new ClassCastException("Ошибка при попытке применения функции к типу.");
    }

    public static Object aAdd(Object a, Object b) {
        if (
                if (a instanceof Integer && b instanceof Integer) {
            (Integer) a + (Integer) b
        } if (a instanceof Float && b instanceof Float) {
            (Float) a + (Float) b
        } if (a instanceof Double && b instanceof Double) {
            (Double) a + (Double) b
        } if (a instanceof String && b instanceof String) {
            (String) a + (String) b
        } throw new ClassCastException("Ошибка при попытке применения функции к типу."); instanceof
        Integer && a instanceof Integer){
            return (Integer) if (a instanceof Integer && b instanceof Integer) {
                (Integer) a + (Integer) b
            } if (a instanceof Float && b instanceof Float) {
                (Float) a + (Float) b
            } if (a instanceof Double && b instanceof Double) {
                (Double) a + (Double) b
            } if (a instanceof String && b instanceof String) {
                (String) a + (String) b
            } throw new ClassCastException("Ошибка при попытке применения функции к типу.");
            +(Integer) a;

        } if ( if (a instanceof Integer && b instanceof Integer) {
            (Integer) a + (Integer) b
        } if (a instanceof Float && b instanceof Float) {
            (Float) a + (Float) b
        } if (a instanceof Double && b instanceof Double) {
            (Double) a + (Double) b
        } if (a instanceof String && b instanceof String) {
            (String) a + (String) b
        } throw new ClassCastException("Ошибка при попытке применения функции к типу."); instanceof
        Float && a instanceof Float){
            return (Float) if (a instanceof Integer && b instanceof Integer) {
                (Integer) a + (Integer) b
            } if (a instanceof Float && b instanceof Float) {
                (Float) a + (Float) b
            } if (a instanceof Double && b instanceof Double) {
                (Double) a + (Double) b
            } if (a instanceof String && b instanceof String) {
                (String) a + (String) b
            } throw new ClassCastException("Ошибка при попытке применения функции к типу.");
            +(Float) a;

        } if ( if (a instanceof Integer && b instanceof Integer) {
            (Integer) a + (Integer) b
        } if (a instanceof Float && b instanceof Float) {
            (Float) a + (Float) b
        } if (a instanceof Double && b instanceof Double) {
            (Double) a + (Double) b
        } if (a instanceof String && b instanceof String) {
            (String) a + (String) b
        } throw new ClassCastException("Ошибка при попытке применения функции к типу."); instanceof
        Double && a instanceof Double){
            return (Double) if (a instanceof Integer && b instanceof Integer) {
                (Integer) a + (Integer) b
            } if (a instanceof Float && b instanceof Float) {
                (Float) a + (Float) b
            } if (a instanceof Double && b instanceof Double) {
                (Double) a + (Double) b
            } if (a instanceof String && b instanceof String) {
                (String) a + (String) b
            } throw new ClassCastException("Ошибка при попытке применения функции к типу.");
            +(Double) a;

        } if ( if (a instanceof Integer && b instanceof Integer) {
            (Integer) a + (Integer) b
        } if (a instanceof Float && b instanceof Float) {
            (Float) a + (Float) b
        } if (a instanceof Double && b instanceof Double) {
            (Double) a + (Double) b
        } if (a instanceof String && b instanceof String) {
            (String) a + (String) b
        } throw new ClassCastException("Ошибка при попытке применения функции к типу."); instanceof
        String && a instanceof String){
            return (String) if (a instanceof Integer && b instanceof Integer) {
                (Integer) a + (Integer) b
            } if (a instanceof Float && b instanceof Float) {
                (Float) a + (Float) b
            } if (a instanceof Double && b instanceof Double) {
                (Double) a + (Double) b
            } if (a instanceof String && b instanceof String) {
                (String) a + (String) b
            } throw new ClassCastException("Ошибка при попытке применения функции к типу.");
            +(String) a;

        } throw new ClassCastException("Ошибка при попытке применения функции к типу.");
    }

    public static Object subtract(Object a, Object b) {
        return a - b;
    }

    public static Object multiply(Object a, Object b) {
        return a * b;
    }

    public static Object divide(Object a, Object b) {
        return a / b;
    }

    public static void main(String[] args) {
        Function<Integer, Integer> lambdaFunction0 = (x) -> {
            return x * x;
        };
        lambdaFunction0.apply(5);
        add(5, 3);
        subtract(10, 4);
        multiply(2, 6);
        divide(8, 2);
        System.out.println("dddqddq");
?x = 10;
?y = 5;
        System.out.println(x + y);
    }
}